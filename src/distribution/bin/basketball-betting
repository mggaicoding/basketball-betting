#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec java -cp "$SCRIPT_DIR/../app/basketball-betting.jar:$SCRIPT_DIR/../lib/*" \
  com.hkjc.training.betting.BasketballBettingDemoApplicationKt "$@"
