spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/betting}
spring.datasource.username=${DATABASE_USERNAME:betting}
spring.datasource.password=${DATABASE_PASSWORD:betting}
spring.flyway.enabled=true
